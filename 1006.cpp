#include <iostream>
#include <vector>

int main(){
    int T, N, K, W;
    std::vector<int> indegree;
    std::vector<int> dp;
    std::vector<int> v;
    std::cin >> T;
    for(int i = 0; i < T; i++){
        int N, T;
        std::cin >> N >> T;
        for(int j = 0; j < N; j++){
            int D;
            std::cin >> D;
            dp[j] = D;
        }
        
    }
}